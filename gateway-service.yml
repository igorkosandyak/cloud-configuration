server:
  port: 1111
zuul:
  host:
    socket-timeout-millis: 30000
ribbon:
  ReadTimeout: 10000
hystrix:
  command:
    default:
      execution:
        isolation:
          strategy: THREAD
          thread:
            timeoutInMilliseconds: 10000